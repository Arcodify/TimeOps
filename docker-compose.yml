version: "3.9"

services:
  hrbot:
    build: .
    container_name: discord-hr-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./exports:/app/exports
      - ./logs:/app/logs
